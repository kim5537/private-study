//1. 사용자에게 값을 3개 받아야한다.
//2. 세가지 값 중  두값을 더하고 나머지 한 값과 비교하여 두값의 합이 한 값의 길이보다 긴지 확인하다.
//3. 2번의 조건을 3번 반복하여 제일 작은 두값을 더한 값이 긴 값의 길이보다 길어야한다.

//1에 필요한  것은 N(prompt)와 이를 반복하는 for함수가 필요하다. - 이때
//2에 필요한 것은 if문을 사용하여 참의 값을 받는 것이다.

const line01 = Number(prompt("첫 변의 길이"));
const line02 = Number(prompt("두번쨰 변의 길이"));
const line03 = Number(prompt("세번째 변의 길이"));

if (line01 > line02) {
  if (line01 > line03) {
  }
} else {
  function val() {}
}
